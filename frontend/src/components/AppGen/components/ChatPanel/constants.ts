import { AGENT_MODE_CONFIG } from '../../lib/stages'

/** Stage IDs and labels for the pipeline. */
export const AGENT_MODES = AGENT_MODE_CONFIG

export const DEFAULT_FREE_TEXT_PLACEHOLDER =
  'If none of these options fit, type your answer here.'
