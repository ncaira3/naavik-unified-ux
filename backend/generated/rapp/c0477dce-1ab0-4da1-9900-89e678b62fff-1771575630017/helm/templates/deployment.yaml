apiVersion: apps/v1
kind: Deployment
metadata:
  name: builder-c0477dce1a
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: builder-c0477dce1a
  template:
    metadata:
      labels:
        app: builder-c0477dce1a
    spec:
      containers:
        - name: rapp-runtime
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
