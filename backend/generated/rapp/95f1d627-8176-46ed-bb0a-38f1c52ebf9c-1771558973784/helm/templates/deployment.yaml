apiVersion: apps/v1
kind: Deployment
metadata:
  name: builder-95f1d62781
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: builder-95f1d62781
  template:
    metadata:
      labels:
        app: builder-95f1d62781
    spec:
      containers:
        - name: rapp-runtime
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
