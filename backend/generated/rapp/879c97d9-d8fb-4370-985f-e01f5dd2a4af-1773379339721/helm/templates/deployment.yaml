apiVersion: apps/v1
kind: Deployment
metadata:
  name: builder-879c97d9d8
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: builder-879c97d9d8
  template:
    metadata:
      labels:
        app: builder-879c97d9d8
    spec:
      containers:
        - name: rapp-runtime
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
