apiVersion: apps/v1
kind: Deployment
metadata:
  name: builder-f751f973f0
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: builder-f751f973f0
  template:
    metadata:
      labels:
        app: builder-f751f973f0
    spec:
      containers:
        - name: rapp-runtime
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
