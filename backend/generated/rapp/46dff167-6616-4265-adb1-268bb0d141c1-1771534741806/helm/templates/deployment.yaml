apiVersion: apps/v1
kind: Deployment
metadata:
  name: builder-46dff16766
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: builder-46dff16766
  template:
    metadata:
      labels:
        app: builder-46dff16766
    spec:
      containers:
        - name: rapp-runtime
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
